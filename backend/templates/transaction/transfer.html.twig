{% extends 'base.html.twig' %}

{% block title %}{{ 'transaction.make_transfer'|trans({}, 'transaction') }} - {{ 'app.name'|trans({}, 'app') }}{% endblock %}

{% block body %}
{% embed '_partials/form_card.html.twig' with {
    title: 'transaction.make_transfer'|trans({}, 'transaction'),
    icon: 'arrow-left-right',
    color: 'primary',
    description: 'dashboard.make_transfer_desc'|trans({}, 'dashboard'),
    cancelRoute: 'customer_dashboard',
    submitText: 'dashboard.transfer_money'|trans({}, 'dashboard'),
    submitIcon: 'check-circle',
    form: form
} %}
    {% block form_fields %}
        <div class="mb-3">
            <div class="form-group">
                {{ form_label(form.fromBankAccountId) }}
                {{ form_widget(form.fromBankAccountId, {'attr': {'class': 'form-select'}}) }}
                {{ form_errors(form.fromBankAccountId) }}
                <small class="form-text text-muted">{{ 'transaction.help_select_source_account'|trans({}, 'transaction') }}</small>
            </div>
        </div>

        <div class="mb-3">
            <div class="form-group">
                {{ form_label(form.toIban) }}
                {{ form_widget(form.toIban, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.toIban) }}
                <small class="form-text text-muted">{{ 'transaction.help_enter_destination_iban'|trans({}, 'transaction') }}</small>
            </div>
        </div>

        <div class="mb-3">
            <div class="form-group">
                {{ form_label(form.amount) }}
                <div class="input-group">
                    {{ form_widget(form.amount, {'attr': {'class': 'form-control'}}) }}
                </div>
                {{ form_errors(form.amount) }}
                <small class="form-text text-muted">{{ 'transaction.help_enter_amount'|trans({}, 'transaction') }}</small>
            </div>
        </div>

        <div class="alert alert-info">
            <i class="bi bi-info-circle"></i>
            {{ 'transaction.currency_exchange_note'|trans({}, 'transaction') }}
        </div>
    {% endblock %}
{% endembed %}
{% endblock %}
